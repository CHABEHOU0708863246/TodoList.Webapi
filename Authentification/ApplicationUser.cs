using Microsoft.AspNetCore.Identity;

namespace TodoList.Webapi.Authentification
{
    public class ApplicationUser : IdentityUser // IdentityUser est une classe qui contient les propriétés de l'utilisateur qui fait partie d’AspNetCore Identity
    {
    }
}
